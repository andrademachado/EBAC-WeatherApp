import styled from 'styled-components';
import { colors } from "../../styles/colors";

export const Header = styled.header`
    margin-left:8px;
    height:72px;
    width : 100%;
    color :red ;
    background-color: ${colors.cabecalho};
    align-items: center;
`