import styled from "styled-components";
import { colors } from "../../styles/colors";

export const LoadDays = styled.div`
    background-color: ${colors.containerBg};
    max-width: 1440px;
    width: 100%;
    height: 124px;
    position: fixed;
    z-index: 2;
`